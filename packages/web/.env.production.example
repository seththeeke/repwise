# Copy to .env.production and fill Cognito + Lambda URL from CDK stack outputs (or AWS console).
# Production hostnames for Repwise (see packages/cdk).

VITE_API_BASE_URL=https://api.repwisefit.com
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_AI_WORKOUT_STREAM_URL=
