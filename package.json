{
  "name": "repwise",
  "version": "0.1.0",
  "private": true,
  "description": "Repwise — fitness app for creating and tracking workouts",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r exec -- rm -rf dist .turbo node_modules 2>/dev/null; rm -rf node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2",
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
