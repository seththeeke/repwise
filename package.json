{
  "name": "repwise",
  "version": "0.1.0",
  "private": true,
  "description": "Repwise — fitness app for creating and tracking workouts",
  "scripts": {
    "build": "pnpm -r build",
    "build:ios": "pnpm --filter web build && pnpm --filter web cap:sync",
    "cap:sync": "pnpm --filter web cap:sync",
    "clean": "pnpm -r exec -- rm -rf dist .turbo node_modules 2>/dev/null; rm -rf node_modules",
    "clear-user-data": "bash scripts/clear-user-data.sh",
    "clear-test-user-data": "bash scripts/clear-test-user-data.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2",
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
